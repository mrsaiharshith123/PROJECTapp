/** Numeric caps for the Free tier (Pro/Power = unlimited where applicable). */
export const FREE_TIER_LIMITS = {
  activeLendingRecords: 5,
  activeChitRecords: 2,
  activeGoals: 3,
  billSplitsPerMonth: 5,
  billSplitParticipants: 3,
  cashflowDays: 30,
};

export const PRO_CASHFLOW_DAYS = 90;
